<!-- Algo permettant de générer un ID pour le compte bancaire, que aucun autre objet ne posséde
soit en générant un chiffre et en vérifiant qu'il n'existe pas
soit essayer de voler la méthode de Valentin: vérifier si des objets existent déja , incrémenter de 1 à partir de là

format 11 chiffres -->
<?php
$rand=0;

function randcompteb($rand){
$strlengthcomptebanc = 11;
$rand = random_int(0, 99999999999);
$rand =substr("00000000000{$rand}",-$strlengthcomptebanc);
return $rand;
};
$comptebancrand =randcompteb($rand);
echo ($comptebancrand);